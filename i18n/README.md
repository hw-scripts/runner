# Interface dictionaries

Each JSON file contains the interface strings for its language code.

`HWhelper.js` loads the selected dictionary from this directory through the repository's raw GitHub URL and caches the most recently loaded copy in browser storage. Keep the same set of keys in every language file. `uk.json` currently mirrors the English dictionary and is ready to be translated.
